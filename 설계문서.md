# 타닥타닥 - 초등학생 타자연습 프로그램 설계 문서

## 1. 프로젝트 정보

| 항목 | 내용 |
|------|------|
| 프로젝트명 | 타닥타닥 |
| 버전 | 1.0.0 |
| 대상 | 초등학생 |
| 배포 환경 | GitHub Pages |
| 기술 스택 | HTML5 / CSS3 / Vanilla JavaScript (ES Modules) |
| 작성일 | 2026-04-17 |

---

## 2. 파일 구조

```
/
├── index.html              # 메인 진입점 (단계 선택 화면)
├── practice.html           # 연습 화면
├── result.html             # 결과 화면
├── css/
│   ├── reset.css           # 기본 스타일 초기화
│   ├── common.css          # 공통 스타일 (색상 변수, 폰트, 버튼, 카드)
│   ├── main.css            # 메인 화면 스타일 (단계 카드 그리드)
│   ├── practice.css        # 연습 화면 스타일 (문장 표시, 키보드)
│   └── result.css          # 결과 화면 스타일
├── js/
│   ├── data.js             # 단계 메타데이터 + 연습 텍스트
│   ├── storage.js          # localStorage / sessionStorage 입출력
│   ├── typing-engine.js    # 문자열 비교, 타수/정확도 계산
│   ├── keyboard.js         # 키보드 시각화
│   ├── main.js             # 메인 화면 로직
│   ├── practice.js         # 연습 화면 로직
│   └── result.js           # 결과 화면 로직
└── assets/
    ├── fonts/              # 웹폰트 (Noto Sans KR)
    └── sounds/             # 타이핑 효과음 (선택)
```

> 모든 JS 파일은 `<script type="module">`로 로드하며, ES Module(`import`/`export`) 형식으로 작성한다.

---

## 3. 단계 시스템 설계

### 3.1 단계 정의

| 단계 ID | 단계명 | 연습 키 | 잠금 해제 조건 |
|---------|--------|---------|---------------|
| `stage1` | 기본자리 | `a s d f g h j k l ;` | 기본 개방 |
| `stage2` | 윗자리 | `q w e r t y u i o p` | stage1 최고타수 ≥ 100 KPM |
| `stage3` | 아랫자리 | `z x c v b n m , . /` | stage2 최고타수 ≥ 100 KPM |
| `stage4` | 짧은글 | 전체 키 | stage3 최고타수 ≥ 120 KPM |

### 3.2 단계 잠금 해제 로직

```
연습 완료
    → KPM 계산 (최종 입력 문자열 길이 기준)
    → 현재 최고타수/정확도 갱신 여부 확인
    → 다음 단계 잠금 해제 조건 충족 여부 확인
    → 조건 충족 시 다음 단계 unlocked = true
    → localStorage 저장
    → sessionStorage에 직전 결과 저장 후 result.html 이동
```

### 3.3 연습 종료 조건

- 입력 문자열이 목표 문자열과 **완전히 일치**할 때만 종료
- 길이가 같아도 오타가 있으면 종료하지 않음
- 종료 전까지 백스페이스로 수정 가능

---

## 4. 데이터 구조

### 4.1 localStorage 스키마

키: `tadak_progress`

```json
{
  "stage1": { "unlocked": true,  "bestKpm": 145, "bestAccuracy": 98, "playCount": 12 },
  "stage2": { "unlocked": true,  "bestKpm": 112, "bestAccuracy": 95, "playCount": 7  },
  "stage3": { "unlocked": false, "bestKpm": 0,   "bestAccuracy": 0,  "playCount": 0  },
  "stage4": { "unlocked": false, "bestKpm": 0,   "bestAccuracy": 0,  "playCount": 0  }
}
```

### 4.2 sessionStorage 스키마

키: `tadak_last_result` (결과 화면 렌더링용 임시 데이터)

```json
{
  "stageId": "stage1",
  "stageOrder": 1,
  "kpm": 112,
  "accuracy": 96,
  "elapsedSeconds": 83,
  "isNewBest": true,
  "previousBestKpm": 98,
  "unlockedStageId": "stage2"
}
```

> 결과 화면은 URL 쿼리 수치 대신 sessionStorage 값을 기준으로 렌더링한다.
> URL 조작으로 점수를 위변조하는 것을 방지하기 위함이다.

### 4.3 `data.js` 구조

```javascript
export const STAGES = [
  {
    id: "stage1",
    order: 1,
    name: "기본자리",
    keys: ["a", "s", "d", "f", "g", "h", "j", "k", "l", ";"],
    unlock: null
  },
  {
    id: "stage2",
    order: 2,
    name: "윗자리",
    keys: ["q", "w", "e", "r", "t", "y", "u", "i", "o", "p"],
    unlock: { stageId: "stage1", minKpm: 100 }
  },
  {
    id: "stage3",
    order: 3,
    name: "아랫자리",
    keys: ["z", "x", "c", "v", "b", "n", "m", ",", ".", "/"],
    unlock: { stageId: "stage2", minKpm: 100 }
  },
  {
    id: "stage4",
    order: 4,
    name: "짧은글",
    keys: [],
    unlock: { stageId: "stage3", minKpm: 120 }
  }
];

export const TEXTS = {
  stage1: [
    "아나 아나 라나 라나",
    "달 달 날 날 말 말",
    "나라 나라 다라 다라",
    // 20개 이상
  ],
  stage2: [
    "피아 요이 파요 야유",
    "우리 우리 여기 여기",
    // ...
  ],
  stage3: [
    "참외 찻잔 축구 파출",
    "참치 축제 최초 초점",
    // ...
  ],
  stage4: [
    "나는 학교에 간다",
    "오늘 날씨가 맑다",
    "친구와 함께 공부해요",
    // ...
  ]
};
```

---

## 5. 화면별 설계

### 5.1 메인 화면 (`index.html`)

```
┌─────────────────────────────────────┐
│            🎹 타닥타닥               │
│         초등학생 타자연습             │
├─────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐        │
│  │ 1단계    │  │ 2단계    │        │
│  │ 기본자리  │  │ 윗자리   │        │
│  │ 최고: 145 │  │ 🔒 잠김  │        │
│  │ [시작하기]│  │ 100타 필요│        │
│  └──────────┘  └──────────┘        │
│  ┌──────────┐  ┌──────────┐        │
│  │ 3단계    │  │ 4단계    │        │
│  │ 아랫자리  │  │ 짧은글   │        │
│  │ 🔒 잠김  │  │ 🔒 잠김  │        │
│  └──────────┘  └──────────┘        │
│              [기록 초기화]           │
└─────────────────────────────────────┘
```

**DOM 구조:**
```html
<main class="main-page">
  <header class="hero"></header>
  <section class="stage-grid" id="stageGrid"></section>
  <button id="resetButton" type="button">기록 초기화</button>
</main>
```

**동작:**
- 잠긴 단계 클릭 시 해제 조건 안내문 표시
- 열린 단계 클릭 시 `practice.html?stage=1` 이동 (URL 숫자만, 내부에서 `stage1` 매핑)

### 5.2 연습 화면 (`practice.html`)

```
┌─────────────────────────────────────┐
│  [← 뒤로]   1단계: 기본자리   ⏱ 01:23 │
├─────────────────────────────────────┤
│  [키보드 시각화 - 현재 단계 키 강조]   │
│                                     │
│  ┌─────────────────────────────┐    │
│  │  아나 아나 라나 라나          │    │
│  │  [정][ ][정][오] 라나 라나    │    │
│  └─────────────────────────────┘    │
│                                     │
│  ⌨ 타수: 87 KPM   🎯 정확도: 95%   │
│                                     │
│  진행: ████████░░ 80%              │
└─────────────────────────────────────┘
```

**DOM 구조:**
```html
<main class="practice-page">
  <header class="practice-header"></header>
  <section id="keyboard" class="keyboard"></section>
  <section class="typing-panel">
    <p id="targetText" class="target-text"></p>
    <input id="typingInput" class="typing-input" autocomplete="off" />
  </section>
  <section class="stats"></section>
</main>
```

**텍스트 색상 규칙:**

| 상태 | 표시 |
|------|------|
| 아직 입력 안 함 (`pending`) | 회색 |
| 올바르게 입력 (`correct`) | 검정 |
| 잘못 입력 (`wrong`) | 빨간색 배경 |

**동작:**
- URL 파라미터 `?stage=1~4` 로 단계 구분, 내부에서 `stage1~stage4` 로 매핑
- 페이지 로드 시 3초 카운트다운 후 타이머 시작
- 붙여넣기(`paste` 이벤트) 차단
- 입력 문자열 = 목표 문자열 완전 일치 시 결과 저장 후 `result.html?stage=1` 이동

### 5.3 결과 화면 (`result.html`)

```
┌─────────────────────────────────────┐
│            🎉 연습 완료!             │
├─────────────────────────────────────┤
│         ⌨ 타수: 112 KPM            │
│         🎯 정확도: 96%              │
│         ⏱ 소요시간: 1분 23초        │
│                                     │
│  🏆 최고 기록 갱신! (기존: 98 KPM)  │  ← isNewBest 시 표시
│  🔓 2단계 윗자리 해제!              │  ← unlockedStageId 있을 때 표시
│                                     │
│  [다시 연습]    [단계 선택으로]       │
└─────────────────────────────────────┘
```

**DOM 구조:**
```html
<main class="result-page">
  <section class="result-card">
    <h1>연습 완료!</h1>
    <div id="resultSummary"></div>
    <div id="resultMessages"></div>
    <div class="result-actions"></div>
  </section>
</main>
```

**동작:**
- `sessionStorage.tadak_last_result` 값 기준으로 렌더링
- sessionStorage 값 없으면 `index.html`로 리다이렉트
- [다시 연습] → `practice.html?stage=n`
- [단계 선택] → `index.html`

---

## 6. 화면 흐름

```
index.html (메인)
    │
    ├─ 열린 단계 카드 클릭
    │
    ▼
practice.html?stage={1~4} (연습)
    │
    ├─ 목표 문자열 완전 일치
    │     → sessionStorage에 결과 저장
    │
    ▼
result.html?stage={1~4} (결과)
    │
    ├─ [다시 연습] → practice.html?stage={n}
    └─ [단계 선택] → index.html
```

---

## 7. 모듈 설계

### 7.1 `storage.js`

```javascript
const STORAGE_KEY = "tadak_progress";
const LAST_RESULT_KEY = "tadak_last_result";

export function createDefaultProgress() {}  // 초기 진행 데이터 생성
export function loadProgress() {}           // localStorage에서 불러오기
export function saveProgress(progress) {}   // localStorage에 저장
export function getStageProgress(stageId) {} // 특정 단계 데이터 반환
export function updateStageResult(stageId, result) {}  // 결과 반영 + 잠금 해제 처리
export function saveLastResult(result) {}   // sessionStorage에 직전 결과 저장
export function loadLastResult() {}         // sessionStorage에서 직전 결과 불러오기
export function resetProgress() {}          // 전체 초기화
```

**`updateStageResult(stageId, result)` 처리 순서:**
1. progress 로드
2. `playCount + 1`
3. `bestKpm`, `bestAccuracy` 갱신 여부 계산
4. 다음 단계 잠금 해제 조건 충족 여부 확인 → `unlocked = true`
5. 저장
6. 결과 화면용 부가 정보 반환

**반환 예시:**
```javascript
{ isNewBest: true, previousBestKpm: 98, unlockedStageId: "stage2" }
```

> localStorage 파싱 실패 시 `createDefaultProgress()`로 복구한다.

### 7.2 `typing-engine.js`

```javascript
export class TypingEngine {
  constructor(targetText) {}
  update(inputText) {}      // 입력값 비교 → 상태 반환
  getKpm(elapsedSeconds) {} // 확정 문자열 길이 기준 KPM 계산
  getAccuracy() {}          // 정확도 계산 (0~100)
  getProgress() {}          // 진행률 계산 (0~100)
  isComplete() {}           // 완전 일치 여부
}
```

**KPM 계산 방식:**
```
KPM = (입력한 총 글자 수 / 경과 시간(초)) × 60
```
- 한글 IME 조합 중간값은 무시하고, `input` 이벤트 시점의 **확정 문자열 길이**만 사용

**`update(inputText)` 반환 구조:**
```javascript
{
  correctCount: 4,
  wrongCount: 1,
  remainingCount: 5,
  progress: 50,
  completed: false,
  statusList: [
    { char: "아", status: "correct" },
    { char: "나", status: "correct" },
    { char: " ", status: "correct" },
    { char: "아", status: "wrong" },
    { char: "나", status: "pending" }
  ]
}
```

**오타 처리 방식:**
- 같은 인덱스 위치의 글자를 비교하는 정적 비교 방식
- 오타가 있는 위치만 `wrong`으로 표시
- 백스페이스로 수정 가능

### 7.3 `keyboard.js`

```javascript
export class Keyboard {
  constructor(container) {}
  render() {}                    // QWERTY 배열 DOM 생성
  highlightStageKeys(keys) {}    // 단계 해당 키 강조
  pressKey(key) {}               // 키 누름 상태 표시
  releaseKey(key) {}             // 키 놓음 상태 복구
  resetPressed() {}              // 전체 pressed 상태 초기화
}
```

- 1차 구현: 단계별 키 강조 중심
- `keydown`/`keyup` 이벤트로 pressed 효과는 보조 기능으로 처리
- 한글 IME 환경에서 눌린 영문 키 추적은 브라우저별 차이가 있으므로 pressed 효과는 보조 기능으로 본다

---

## 8. UI / 디자인 가이드

### 색상 팔레트

| 용도 | 색상 | 코드 |
|------|------|------|
| 주 색상 | 하늘색 | `#4FC3F7` |
| 강조 색상 | 노란색 | `#FFD54F` |
| 잠금 색상 | 회색 | `#B0BEC5` |
| 오타 색상 | 빨간색 | `#EF5350` |
| 배경 | 연한 하늘 | `#E1F5FE` |
| 텍스트 | 진한 남색 | `#1A237E` |

### 폰트
- 본문: `Noto Sans KR` (Google Fonts CDN)
- 크기: 최소 18px (초등학생 기준 가독성)

### 반응형
- 태블릿 우선 설계 (학교 환경 고려)
- 최소 지원 해상도: 768px 이상

---

## 9. 기술 제약 및 결정

| 결정 사항 | 이유 |
|-----------|------|
| 순수 JS (프레임워크 없음) | GitHub Pages 정적 배포, 빌드 과정 불필요 |
| ES Modules (`import`/`export`) | 파일 간 의존성 명확하게 관리 |
| 멀티 페이지 (SPA 아님) | 구조 단순, 각 화면 URL 명확 |
| localStorage 저장 | 서버 없이 진행 상황 유지 |
| sessionStorage로 결과 전달 | URL 수치 조작으로 인한 위변조 방지 |
| 서버 없음 | 유지보수 비용 없음, GitHub Pages 무료 운영 |

---

## 10. 구현 범위

### 1차 필수

- 메인 화면 단계 카드 표시 (잠금/해제 상태 반영)
- localStorage 기반 잠금/해제 상태 영속
- 단계별 연습 텍스트 랜덤 선택
- 3초 카운트다운 후 타이핑 시작
- 실시간 정확도 / KPM / 진행률 표시
- 완료 후 결과 저장 및 결과 화면 표시
- 최고 기록 갱신
- 조건 충족 시 다음 단계 해제

### 2차 권장

- 키보드 시각화
- 잠긴 단계 클릭 시 조건 안내
- 기록 초기화 버튼
- 태블릿/모바일 레이아웃 다듬기

### 3차 선택

- 타건 효과음
- 애니메이션
- 축하 이펙트

---

## 11. 마일스톤 (개발 순서)

| 단계 | 작업 | 완료 기준 |
|------|------|----------|
| M1 | 폴더 구조 생성, 공통 CSS 변수, HTML 3개 기본 레이아웃 | 빈 화면 3개가 정상 로드 |
| M2 | `data.js`, `storage.js` 구현 | 콘솔에서 load/save/reset 확인, 새로고침 후 유지 |
| M3 | 메인 화면 (`main.js`) | 저장 상태에 따라 카드 UI 즉시 반영 |
| M4 | `typing-engine.js` 구현 | 콘솔에서 단위 테스트 (정확도, KPM, 완료 판정) |
| M5 | 연습 화면 (`practice.js`) | 입력→완료→결과 저장 흐름 동작 |
| M6 | 결과 화면 (`result.js`) | 직전 기록 안정적으로 표시, 잠금 해제 메시지 정상 출력 |
| M7 | `keyboard.js` 키보드 시각화 | 단계별 강조 표시 |
| M8 | 애니메이션 / 효과음 | 선택 |
| M9 | GitHub Pages 배포 | 외부 URL에서 정상 접속 |

---

## 12. 구현 시 주의 리스크

| 리스크 | 대책 |
|--------|------|
| 한글 IME 조합 이벤트 차이 | `keydown` 값 직접 비교 금지. 입력 필드 `value` 기준으로만 비교 |
| 결과 수치 URL 위변조 | 결과 화면은 `sessionStorage` 기준으로 렌더링, URL에는 `stage`만 포함 |
| localStorage 파싱 실패 | try-catch 후 `createDefaultProgress()`로 자동 복구 |
| 텍스트 품질 | stage1~3 텍스트는 해당 줄 키만 포함된 단어로만 구성 |
| 붙여넣기로 타수 조작 | `paste` 이벤트 차단 |

---

## 13. 구현 완료 체크리스트

- [ ] 첫 실행 시 stage1만 열려 있다
- [ ] stage1에서 100 KPM 이상이면 stage2가 열린다
- [ ] stage2에서 100 KPM 이상이면 stage3가 열린다
- [ ] stage3에서 120 KPM 이상이면 stage4가 열린다
- [ ] 최고 기록은 새 기록이 더 높을 때만 갱신된다
- [ ] 정확도와 KPM이 실시간으로 변한다
- [ ] 문장을 정확히 입력해야만 결과 화면으로 이동한다
- [ ] 오타가 있는 상태에서 엔터나 완료 처리가 되지 않는다
- [ ] 새로고침 후에도 단계 해제 상태가 유지된다
- [ ] 결과 화면 직접 접근 시 index.html로 리다이렉트된다
- [ ] 다시 연습과 메인 이동 버튼이 정상 동작한다
- [ ] 붙여넣기가 차단된다
