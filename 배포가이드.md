# 타닥타닥 배포 가이드

## 1. 배포 방식

이 프로젝트는 정적 웹사이트라서 GitHub Pages에 바로 배포할 수 있다. 빌드 과정은 필요 없고, 현재 폴더 그대로 업로드하면 된다.

배포 대상 파일:

- `index.html`
- `practice.html`
- `result.html`
- `css/`
- `js/`
- `image/`
- `.nojekyll`

배포 제외:

- `node_modules/`
- `package-lock.json`
- 문서 파일들(`설계문서.md`, `구현계획.md`, `개발현황.md`)은 올려도 되지만 필수는 아님

## 2. 로컬 테스트

프로젝트 폴더에서 실행:

```powershell
npm run dev
```

브라우저 주소:

```text
http://127.0.0.1:4173/index.html
```

## 3. GitHub Pages 배포 순서

### 방법 A. GitHub 웹사이트에서 업로드

1. GitHub에서 새 저장소 생성
2. 이 폴더의 파일을 업로드
3. 저장소의 `Settings > Pages` 이동
4. `Build and deployment`에서 아래처럼 설정
   - `Source`: `Deploy from a branch`
   - `Branch`: `main` / `/ (root)`
5. 저장 후 배포 완료까지 잠시 대기
6. 배포 주소 접속

예시 주소:

```text
https://사용자이름.github.io/저장소이름/
```

### 방법 B. Git으로 업로드

```powershell
git init
git add .
git commit -m "Initial deploy"
git branch -M main
git remote add origin https://github.com/사용자이름/저장소이름.git
git push -u origin main
```

그 다음 GitHub Pages를 `main` 브랜치 root 기준으로 켜면 된다.

## 4. 배포 전 체크리스트

- `index.html`이 첫 화면으로 열린다
- 메인에서 단계 선택이 된다
- 30초 연습이 정상 동작한다
- 문장 완료 후 자동 전환이 아니라 `스페이스`로 다음 문장 이동한다
- 시간이 끝나면 `스페이스`로 결과 화면 이동한다
- 새로고침 후 기록이 유지된다

## 5. 주의할 점

- `localStorage`와 `sessionStorage`를 쓰므로 브라우저별로 기록은 각자 따로 저장된다
- 다른 기기와 기록이 자동 동기화되지는 않는다
- GitHub Pages는 HTTPS로 열리므로 별도 서버는 필요 없다

## 6. 현재 상태 메모

- 이 폴더는 아직 Git 저장소로 초기화되어 있지 않다
- 그래서 여기서 바로 원격 배포까지는 할 수 없고, 저장소를 만든 뒤 업로드가 필요하다
- 프로젝트 구조 자체는 GitHub Pages에 올리기 적합한 상태다
