# 타닥타닥 구현 계획

## 1. 설계문서 검토 요약

설계문서는 화면 흐름, 단계 구조, 저장 스키마가 잘 잡혀 있어 바로 구현을 시작할 수 있는 수준이다. 다만 실제 개발 전에 아래 항목은 명확히 고정해 두는 편이 안전하다.

### 확정이 필요한 항목

1. `stage` 파라미터 형식
   - 설계문서에는 `practice.html?stage=1` 예시와 `stage1` 형태가 함께 나온다.
   - 구현에서는 URL은 `1~4`로 단순화하고, 내부 데이터 키는 `stage1~stage4`로 매핑하는 방식이 가장 다루기 쉽다.

2. 결과 화면 데이터 전달 방식
   - `result.html?stage={1~4}&kpm={n}&accuracy={n}` 만으로는 새로고침 시 조작 가능성이 있다.
   - 구현은 `sessionStorage.lastResult`에 직전 결과를 저장하고, 결과 화면은 이 값을 우선 사용한다.
   - URL에는 `stage` 정도만 남기고, 점수 값은 화면 렌더링용 임시 데이터로 관리한다.

3. KPM 계산 기준
   - 현재 설계는 `입력한 총 글자 수 / 경과 시간` 기준이다.
   - 한글 조합형 입력은 브라우저별 조합 이벤트 차이가 있어, 최종 입력 문자열 길이 기준으로 계산하는 쪽이 단순하고 안정적이다.
   - 즉, 조합 중간값은 무시하고 `input` 이벤트에서 확정 문자열만 비교한다.

4. 오타 처리 방식
   - 설계상 "잘못 입력한 글자: 빨간색 배경"이 있으나, 오타 후 다음 글자로 진행 가능한지 명확하지 않다.
   - 1차 구현은 "입력 문자열과 목표 문자열의 같은 인덱스를 비교"하는 정적 비교 방식으로 간다.
   - 즉, 사용자가 틀리게 입력하면 그 위치만 오타로 표시되고, 백스페이스로 수정 가능하게 만든다.

5. 연습 종료 조건
   - 목표 문자열과 입력 문자열이 완전히 같을 때 종료한다.
   - 길이만 같고 오타가 있으면 종료하지 않는다.

## 2. 구현 범위 정의

### 1차 필수 범위

- 메인 화면에서 단계 카드 표시
- localStorage 기반 잠금/해제 상태 반영
- 단계별 연습 텍스트 랜덤 선택
- 카운트다운 후 타이핑 시작
- 실시간 정확도, KPM, 진행률 표시
- 완료 후 결과 저장 및 결과 화면 표시
- 최고 기록 갱신
- 조건 충족 시 다음 단계 해제

### 2차 권장 범위

- 키보드 시각화
- 잠긴 단계 툴팁
- 초기화 버튼
- 모바일/태블릿 레이아웃 다듬기

### 3차 선택 범위

- 타건 효과음
- 간단한 애니메이션
- 축하 이펙트

## 3. 파일별 구현 책임

### HTML

- `index.html`
  - 앱 소개 헤더
  - 단계 카드 4개
  - 초기화 버튼 1개

- `practice.html`
  - 상단 정보 바
  - 키보드 영역
  - 목표 문장/입력 상태 영역
  - 실시간 통계 영역
  - 숨김 또는 시각적 입력 필드

- `result.html`
  - 결과 요약
  - 최고 기록 갱신 메시지
  - 다음 단계 해제 메시지
  - 다시 연습 / 메인 이동 버튼

### CSS

- `css/reset.css`
  - 기본 margin/padding/box-sizing 정리

- `css/common.css`
  - 컬러 변수, 폰트, 버튼, 카드, 공통 레이아웃

- `css/main.css`
  - 단계 카드 그리드
  - 잠금 상태 스타일

- `css/practice.css`
  - 문장 표시 영역
  - 글자 상태별 색상
  - 진행률 바
  - 키보드 시각화

- `css/result.css`
  - 결과 카드
  - 축하/잠금해제 메시지 강조

### JavaScript

- `js/data.js`
  - 단계 메타데이터
  - 연습 문장 목록
  - 잠금 해제 기준값

- `js/storage.js`
  - 기본 진행 데이터 생성
  - load/save/get/update/reset 제공

- `js/typing-engine.js`
  - 문자열 비교
  - 정확도 계산
  - KPM 계산
  - 완료 여부 판정

- `js/keyboard.js`
  - 키보드 DOM 렌더링
  - 단계별 강조
  - 키 입력 시 pressed 상태 반영

- `js/main.js`
  - 저장 상태 불러오기
  - 단계 카드 렌더링
  - 카드 클릭 처리

- `js/practice.js`
  - URL 단계 파싱
  - 현재 문장 선정
  - 카운트다운
  - 입력 이벤트 처리
  - 실시간 UI 갱신
  - 완료 시 결과 저장 후 이동

- `js/result.js`
  - 직전 결과 읽기
  - 기록 비교 결과 표시
  - 버튼 이벤트 연결

## 4. 데이터 구조 구체안

### `js/data.js`

```javascript
export const STAGES = [
  {
    id: "stage1",
    order: 1,
    name: "기본자리",
    queryValue: "1",
    keys: ["a", "s", "d", "f", "g", "h", "j", "k", "l", ";"],
    unlock: null
  },
  {
    id: "stage2",
    order: 2,
    name: "윗자리",
    queryValue: "2",
    keys: ["q", "w", "e", "r", "t", "y", "u", "i", "o", "p"],
    unlock: { stageId: "stage1", minKpm: 100 }
  }
];

export const TEXTS = {
  stage1: [],
  stage2: [],
  stage3: [],
  stage4: []
};
```

### `localStorage`

키: `tadak_progress`

```json
{
  "stage1": { "unlocked": true, "bestKpm": 0, "bestAccuracy": 0, "playCount": 0 },
  "stage2": { "unlocked": false, "bestKpm": 0, "bestAccuracy": 0, "playCount": 0 },
  "stage3": { "unlocked": false, "bestKpm": 0, "bestAccuracy": 0, "playCount": 0 },
  "stage4": { "unlocked": false, "bestKpm": 0, "bestAccuracy": 0, "playCount": 0 }
}
```

### `sessionStorage`

키: `tadak_last_result`

```json
{
  "stageId": "stage1",
  "stageOrder": 1,
  "kpm": 112,
  "accuracy": 96,
  "elapsedSeconds": 83,
  "isNewBest": true,
  "unlockedStageId": "stage2"
}
```

## 5. 모듈 상세 설계

### `storage.js`

```javascript
const STORAGE_KEY = "tadak_progress";
const LAST_RESULT_KEY = "tadak_last_result";

function createDefaultProgress() {}
function loadProgress() {}
function saveProgress(progress) {}
function getStageProgress(stageId) {}
function updateStageResult(stageId, result) {}
function saveLastResult(result) {}
function loadLastResult() {}
function resetProgress() {}
```

#### `updateStageResult(stageId, result)` 처리 순서

1. 현재 progress 로드
2. 해당 단계 `playCount + 1`
3. `bestKpm`, `bestAccuracy` 갱신 여부 계산
4. 다음 단계 잠금 해제 조건 확인
5. 저장
6. 결과 화면에서 쓸 부가정보 반환

반환 예시:

```javascript
{
  isNewBest: true,
  previousBestKpm: 98,
  unlockedStageId: "stage2"
}
```

### `typing-engine.js`

```javascript
export class TypingEngine {
  constructor(targetText) {
    this.targetText = targetText;
    this.inputText = "";
    this.correctCount = 0;
    this.wrongCount = 0;
  }

  update(inputText) {}
  getKpm(elapsedSeconds) {}
  getAccuracy() {}
  getProgress() {}
  isComplete() {}
  getStatusList() {}
}
```

#### `update(inputText)` 반환 구조

```javascript
{
  inputText: "아나 아",
  correctCount: 4,
  wrongCount: 1,
  remainingCount: 5,
  progress: 50,
  completed: false,
  statusList: [
    { char: "아", status: "correct" },
    { char: "나", status: "correct" },
    { char: " ", status: "correct" },
    { char: "아", status: "wrong" },
    { char: "나", status: "pending" }
  ]
}
```

### `keyboard.js`

1차 구현은 실제 물리 키 추적보다 "단계별 키 강조" 중심으로 단순화한다.

```javascript
export class Keyboard {
  constructor(container) {}
  render() {}
  highlightStageKeys(keys) {}
  pressKey(key) {}
  releaseKey(key) {}
  resetPressed() {}
}
```

## 6. 화면 구현 순서

### M1. 기본 골격 세팅

- 폴더 구조 생성
- 공통 CSS 변수 정의
- HTML 3개 기본 레이아웃 작성
- JS 모듈 import 방식 결정

권장:
- `<script type="module">` 사용
- 모든 JS는 ES module 형태로 작성

### M2. 데이터/저장 계층 구현

- `data.js` 작성
- `storage.js` 작성
- 초기 진입 시 progress 자동 생성
- 브라우저 콘솔에서 load/save/reset 확인

완료 기준:
- 새로고침 후에도 진행 정보 유지
- reset 후 초기 상태 복구

### M3. 메인 화면 구현

- 단계 카드 4개 렌더링
- 잠금/해제 상태 반영
- 최고 KPM 표시
- 잠긴 카드 클릭 시 안내문 표시
- 열린 카드 클릭 시 `practice.html?stage=n` 이동

완료 기준:
- 저장 상태에 따라 카드 UI가 즉시 달라진다.

### M4. 타이핑 엔진 구현

- 문자열 비교 로직
- 정확도 계산
- KPM 계산
- 진행률 계산
- 완료 판정

테스트 케이스:
- 완전 일치 입력
- 중간 오타 후 수정
- 빈 입력
- 매우 빠른 입력
- 한글/공백 포함 문장

### M5. 연습 화면 구현

- 단계 파라미터 검증
- 단계별 문장 랜덤 선택
- 3초 카운트다운
- 입력 시작 시 타이머 시작
- 실시간 상태 표시
- 완료 시 결과 저장

주의:
- 타이머는 카운트다운 종료 후 시작
- 붙여넣기 방지는 1차에서 선택 사항이지만, 가능하면 `paste` 이벤트 차단 권장

### M6. 결과 화면 구현

- 직전 결과 표시
- 최고 기록 갱신 여부 표시
- 잠금 해제 메시지 표시
- 다시 연습 / 메인 이동 연결

완료 기준:
- 결과 화면 새로 진입 시 직전 기록이 안정적으로 보인다.

### M7. 키보드 시각화 구현

- QWERTY 배열 렌더링
- 현재 단계 키 강조
- 실제 keydown/keyup 시 pressed 상태 표시

주의:
- 한글 입력 상태에서도 실제 눌린 영문 키 추적은 브라우저/IME 영향이 있으므로, pressed 효과는 보조 기능으로 본다.

## 7. 화면별 DOM 초안

### `index.html`

```html
<main class="main-page">
  <header class="hero"></header>
  <section class="stage-grid" id="stageGrid"></section>
  <button id="resetButton" type="button">기록 초기화</button>
</main>
```

### `practice.html`

```html
<main class="practice-page">
  <header class="practice-header"></header>
  <section id="keyboard" class="keyboard"></section>
  <section class="typing-panel">
    <p id="targetText" class="target-text"></p>
    <input id="typingInput" class="typing-input" autocomplete="off" />
  </section>
  <section class="stats"></section>
</main>
```

### `result.html`

```html
<main class="result-page">
  <section class="result-card">
    <h1>연습 완료!</h1>
    <div id="resultSummary"></div>
    <div id="resultMessages"></div>
    <div class="result-actions"></div>
  </section>
</main>
```

## 8. 구현 시 주의할 리스크

### 1. 한글 IME 조합

- `keydown` 값으로 글자를 직접 비교하면 오류가 날 수 있다.
- 반드시 입력 필드의 실제 value 기준으로 비교한다.

### 2. 결과 위변조

- URL 쿼리에 수치를 직접 싣는 방식은 신뢰하기 어렵다.
- 결과 화면은 `sessionStorage` 값을 기준으로 렌더링한다.

### 3. 데이터 손상

- localStorage 파싱 실패 시 기본값으로 복구하는 예외 처리 필요

### 4. 텍스트 데이터 품질

- stage1~3은 해당 줄의 키 학습 목적에 맞는 단어만 구성해야 한다.
- 잘못 섞인 글자가 있으면 학습 의도가 흐려진다.

## 9. 추천 작업 순서

1. `data.js`, `storage.js`부터 만든다.
2. `index.html`과 `main.js`로 단계 카드 화면을 먼저 완성한다.
3. `typing-engine.js`를 독립 구현하고 콘솔로 테스트한다.
4. `practice.html` 연결 후 입력/완료 흐름을 붙인다.
5. `result.html`과 기록 갱신 메시지를 붙인다.
6. 마지막에 `keyboard.js`와 애니메이션을 추가한다.

## 10. 구현 완료 기준 체크리스트

- 첫 실행 시 stage1만 열려 있다.
- stage1에서 100 KPM 이상이면 stage2가 열린다.
- stage2에서 100 KPM 이상이면 stage3가 열린다.
- stage3에서 120 KPM 이상이면 stage4가 열린다.
- 최고 기록은 새 기록이 더 높을 때만 갱신된다.
- 정확도와 KPM이 실시간으로 변한다.
- 문장을 정확히 입력해야만 결과 화면으로 이동한다.
- 새로고침 후에도 단계 해제 상태가 유지된다.
- 결과 화면에서 다시 연습과 메인 이동이 정상 동작한다.

## 11. 바로 다음 액션

문서 기준으로 바로 구현에 들어간다면 아래 순서가 가장 효율적이다.

1. 프로젝트 폴더 구조 생성
2. `data.js`와 `storage.js` 작성
3. 메인 화면부터 연결
4. 연습 화면과 `typing-engine.js` 연결
5. 결과 화면 및 잠금 해제 흐름 마무리

현재 설계문서는 충분히 괜찮고, 핵심은 "한글 입력 처리"와 "결과 저장 방식"만 위처럼 정리해서 구현하는 것이다.
